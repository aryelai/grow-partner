<!-- devres:start -->
# 项目 Agent 规则

> 由 devres 根据 `wechat-miniprogram` 配置为 `agents-compatible` 的 `project` 作用域生成。请不要手动修改本区块。

## 当前项目事实

- 项目名称：grow-partner
- 项目摘要：「成长搭档」是一款面向初中生家庭的微信小程序，帮助家长集中管理孩子的作业登记、通知提醒、习惯养成打卡和日常计划。支持多家庭成员协作（爸爸/妈妈/孩子各自账号），基于微信云开发实现数据共享与实时同步，内置AI能力用于作业图片识别和语音录入。
- 解决问题：孩子进入初中后，老师通过QQ群发布作业和通知，信息碎片化严重、易遗漏。家长需要一个集中管理工具来：1）登记各科作业并按科目/状态筛选查看；2）记录学校通知并设置定时提醒；3）跟踪驼背矫正等习惯养成打卡；4）制定每日/每周计划。现有通用待办工具无法满足"以孩子为中心、多家庭成员协作、按学期组织"的垂直场景需求。
- 主要用户：爸爸、妈妈、爷爷、奶奶、外公、外婆
- 核心场景：日常作业登记、晚上作业检查与标记完成、学校通知记录与智能提醒、习惯养成打卡、每日计划制定与执行
- 当前阶段：基础版功能开发完成，待云环境部署与真机联调

## 技术栈

- 语言：JavaScript（ES6+，不使用 TypeScript）、WXML、WXSS、JSON
- 运行时：微信小程序运行时、微信云函数 Node.js 运行时
- 框架：微信小程序原生框架、微信云开发（云函数、云数据库、云存储）
- 包管理器：npm（仅用于云函数部署时安装官方 wx-server-sdk 运行时依赖）
- 数据库：微信云数据库（文档型数据库）、微信云存储
- 测试框架：Node.js node:test、项目静态结构验证脚本
- 构建工具：微信开发者工具

## 目录结构

- `miniprogram`：原生微信小程序前端页面、样式与公共工具
- `cloudfunctions`：微信云开发云函数与数据库部署说明
- `tests`：核心业务逻辑自动化测试
- `scripts`：项目静态验证脚本
- `docs`：产品需求与开发执行文档

## 项目命令



### 本地开发

- `/Applications/wechatwebdevtools.app/Contents/MacOS/cli open --project .`：使用微信开发者工具打开当前小程序项目；验证：微信开发者工具成功载入项目且模拟器完成编译

### 测试

- `node --test tests/*.test.js`：运行核心业务逻辑自动化测试
- `node scripts/validate-project.js`：校验页面、云函数与项目结构完整性

### 构建

- 不适用：原生微信小程序由微信开发者工具编译，当前没有独立 CLI 构建命令

## 发布规则

- 不适用：当前没有正式发布流程

## 保护区域

- `.env`：敏感配置（需要确认）

## 项目专属资源

- Skills：miniprogram-ui-review

## 语言规范

- 面向用户的自然语言默认使用简体中文，先给结论，再给必要依据。
- 变量、函数、类、接口和其他代码标识符使用英文，禁止拼音或中英混合命名。
- JSDoc、TSDoc 和代码行内注释使用简体中文；日志与异常信息可以使用英文。
- 命令、路径、协议、配置键、API 字段、库名和无法准确翻译的术语保持原始拼写。
- 用户明确指定其他语言时服从该次要求；不得为语言统一破坏项目命名或第三方接口兼容性。

## 工作流

- 先确认目标、作用域、现有事实、风险和完成标准；能自行查证的信息不转问用户。
- 复杂任务拆成可验证步骤，只询问会实质改变方案或授权边界的问题。
- 修改前检查工作区和相关实现，保留用户已有变更，只处理当前任务直接需要的内容。
- 实施采用最小、聚焦、可回退的修改，不顺手重构或扩展范围。
- 失败时读取完整错误并定位根因，不用随机修改掩盖症状。
- 咨询、解释、审查和状态报告默认只读；高影响写操作必须获得相应授权。
- 完成前运行项目声明且与风险匹配的验证，读取退出状态后再报告结果和剩余限制。

## 安全底线

- 禁止在代码、配置、测试、文档、日志或输出中硬编码、泄露密钥和其他敏感信息。
- 外部输入在信任边界处校验类型、格式、范围和允许值；错误信息不得回显敏感载荷。
- 文件与命令操作必须防止路径穿越、符号链接逃逸、越界写入和 Shell 注入。
- 权限采用最小授权和默认拒绝，不通过关闭校验、鉴权或确认解决问题。
- 数据迁移、批量修改和删除必须具备影响评估、明确授权以及备份、事务或回滚方式。
- 外部请求设置协议限制、超时、大小上限和必要的完整性检查。
- 项目规则可以提高安全要求，不得降低密钥保护、输入校验和平台安全底线。

## 项目执行基线

- 以项目事实中的目录、命令、保护区域和发布状态为准，不用模板示例或经验猜测替代。
- 修改前定位真实入口、相邻实现和适用的更深目录规则；新文件放入已声明的职责目录。
- 只运行项目事实列出的命令；命令未配置或不适用时，说明缺少的验证证据，不虚构替代命令。
- 验证范围与风险匹配，覆盖直接行为、失败路径和受影响构建；读取退出状态后再声明完成。
- 发布、部署、数据迁移和保护区域修改遵守项目事实中的确认与回滚要求。
- 仓库证据与 `devres.project.yaml` 不一致时先报告；只有用户授权后才能更新项目事实并运行升级。

## 项目规则优先级

- 当前明确指令优先于项目规则；项目规则优先于全局默认规则。
- 更深目录规则只覆盖其作用域内的同类事项，未冲突内容继续继承根规则。
- 项目事实、真实命令、保护区域和发布约束不得被全局偏好覆盖。
- 同级规则冲突且会改变实现或安全边界时，停止相关写操作并请求最小必要确认。

## 工具差异

`AGENTS.md` 是跨工具项目规则事实入口；`CLAUDE.md` 只负责导入该入口和承载确有必要的 Claude 专属内容。工具包装形式可以不同，核心项目语义必须一致。

## 按需规则路由

以下规则正文不常驻根指令。当前工具没有自动加载路径规则时，在读取或修改匹配文件前加载对应规则：

- `AGENTS.md`、`CLAUDE.md`、`.devres/**`、`.agents/**`、`.claude/**`：读取 `.devres/rules/project-owned-resources.md`。
<!-- devres:end -->

<!-- devres:user:start -->
<!-- 在此区域添加项目个性化规则；devres 不会修改本区块。 -->
<!-- devres:user:end -->

# 1.0.3 上传记录

## 上传结果（2026-09-15）

- 目标小程序：尹尹成长搭档，AppID `wxb6da94fe8eda779d`。
- 版本：`1.0.3`。
- 上传来源：当前 `codex/ai-homework-import` 工作区，包含尚未提交的作业日期、主题与详细要求展示、科目编辑和手写识别核对改动。
- 使用微信官方开发者工具 CLI 上传；工具输出 `✔ upload`，进程退出码为 0。
- 工具报告代码包大小为 597408 字节，约 583.4 KB。
- 上传备注：作业日期与周表默认日期、完整主题展示、详细要求、科目编辑及手写识别核对优化。
- 上传前最近完整验证：376 项测试通过，结构校验通过，补丁检查通过。

首次命令因沙箱不允许监听本机端口而在初始化阶段退出，没有完成上传；获准在沙箱外运行后，官方工具成功连接既有 IDE 并完成上传。没有关闭域名校验或绕过微信后台的电脑操作限制。

## 正式发布前待办

- [x] 把匹配的 `settings`、`homework`、`ai` 云函数部署到已有 `cloud1` 环境；本次没有发起密钥、限额或权限配置写入。
- [ ] 验证新日期字段可以保存并回读、AI 草稿的日期与主题规则、科目改名及真机界面。
- [x] 把本次工作区改动提交并推送 GitHub；业务代码对应提交为 `3a44bcc`。
- [ ] 由用户在微信后台核对 `1.0.3` 并完成平台要求的代码审核与正式发布。

开发版本代码已上传，匹配云函数已部署并完成代码回读核验，业务代码提交 `3a44bcc` 已推送至 GitHub 分支 `codex/ai-homework-import`。尚未提交审核或发布正式版；真机业务验收仍未完成。

## 真机验收启动记录（2026-09-15）

- 微信开发者工具 `Stable 2.02.2608060` 首次启动 iOS“真机调试”时，代码包上传成功，但最终提示 `真机调试 Error: Timeout [2.02.2608060][darwin-arm64]`，因此该路径未形成真机通过证据。
- 随后改用工具自带的“二维码预览”链路，生成 583 KB 开发版代码包与有效二维码；二维码界面显示有效至 2026-09-15 15:22。
- 预览编译期间，工具因云函数根目录未选择环境提示两次 `ai` 云函数上传失败；此前 `ai` 已通过官方 CLI 部署并完成云端回读，预览包仍成功生成。真机仍需确认实际调用是否正常，不能仅凭既有部署推断通过。
- 等待真实手机扫码后验证：冷启动、作业日期保存与回读、科目改名、AI 导入日期与完整主题、列表与详情显示、键盘及底部安全区。
- 当前只有 iOS 预览二维码；Android 真机、弱网、断网与前后台恢复尚无证据。

## 匹配云函数部署记录（2026-09-15）

用户明确授权后，使用微信官方开发者工具 CLI，分别部署已有环境 `cloud1-d3g2hleood2cae7e7` 的三个目标函数。环境列表只返回该环境；没有创建新环境。

部署命令形式为：

```bash
/Applications/wechatwebdevtools.app/Contents/MacOS/cli cloud functions deploy --project /Users/arye/projects/grow-partner --env cloud1-d3g2hleood2cae7e7 --names settings --remote-npm-install --lang zh
```

随后将 `--names` 分别替换为 `homework`、`ai` 逐个执行。三个命令均明确报告更新已存在函数，返回 `success: true`，退出码 0；没有调用配置修改、数据迁移或删除接口。

| 函数 | 上传文件数 | 工具报告包大小 | 部署后状态 | 运行时 | 超时 |
| --- | --- | --- | --- | --- | --- |
| settings | 2 | 3.4 KB | Active | Nodejs16.13 | 3 秒 |
| homework | 2 | 4.0 KB | Active | Nodejs16.13 | 3 秒 |
| ai | 4 | 14.7 KB | Active | Nodejs16.13 | 120 秒 |

状态、运行时、超时由官方 `cloud functions info` 部署前后回读核对。该命令不返回环境变量；本次未读取密钥值、未发起环境变量修改，不能把可见字段核验宣称为所有云端配置的完整比对。

部署前先下载三个当前函数代码到 `/private/tmp/grow-partner-cloud-1.0.3-xwLWLF/before`，8 个业务和配置文件均与 Git 基线 `49433a0` 一致。部署后重新下载至同目录的 `after`，8 个文件逐字节与本地待发布代码一致。

业务文件 SHA-256 回读证据：

| 文件 | SHA-256 |
| --- | --- |
| settings/index.js | b8fadcf0af3363f0931816a7ac381068f6e731eecbd4e94c49ba7a80f86a63d0 |
| homework/index.js | fa1145b82ce33e35aad18559eb1a457452c56405defac9e992879f17a2398359 |
| ai/index.js | 7f532e8079631b898edcb739461a84dd5724f3cfe066a771f01d5acf48433d19 |
| ai/core.js | 97b8e6dfa4741165c9eeadac9aa410be9dacef5afc0d70898f167cecf1fe0deb |
| ai/model-client.js | 9db6f81f699327093ee7898c66ea2acfe4e714a8a1bcf48fc98d17c6e6ff15fe |

三个 `package.json` 也完全一致。实际下载的云端依赖版本核验通过：`settings`、`homework` 使用 `wx-server-sdk@3.0.1`，`ai` 使用 `wx-server-sdk@4.0.2` 与 `@cloudbase/node-sdk@3.17.2`。

部署前再次执行完整回归：376 项通过、0 项失败或跳过；结构校验为 146 个文件、17 个页面、9 个云函数，通过。没有为验收新增或修改历史作业、科目配置、家庭资料，没有调用付费模型；真实日期保存与回读、科目修改、AI 导入效果及真机界面需在 `1.0.3` 开发版或体验版继续验收。

回退代码可从临时 `before` 备份恢复，也可从已核验的 Git 基线 `49433a0` 重建。临时目录可能被系统清理，不作为长期备份；真实回退需重新确认，不删除新增日期或历史数据。
